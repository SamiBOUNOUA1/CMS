# ─────────────────────────────────────────────────────────────────────────────
# SECURITY TODO (rotate — must be done outside this codebase):
#   1. MONGODB_URI: the database user password below was committed in plaintext.
#      Rotate the Atlas DB-user password and paste the new connection string here.
#   2. ADMIN_PASSWORD: change from the default before/after first deploy.
#   3. WhatsApp access token (stored in DB via /settings/whatsapp) — rotate in Meta.
# Prefer injecting these via your host's secret manager rather than this file.
# ─────────────────────────────────────────────────────────────────────────────
MONGODB_URI=mongodb+srv://bounouasami:7vVA1XgN9L9Jkwsv@cluster0.bw5qsfe.mongodb.net/cms
#MONGODB_URI=mongodb://127.0.0.1:27017/cms
PORT=5000
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/catering-quotes?retryWrites=true&w=majority

# Secret key for JWT signing — strong random value (rotate periodically).
JWT_SECRET=257eb0541efa2577969117ddfb81fe750d44a6e48a6983f2685ad79076d69e94c947c141a5b2a581b143beefcdd2c752
NODE_ENV=production
ADMIN_EMAIL=admin@example.com
# TODO(rotate): replace the default admin password before going live.
ADMIN_PASSWORD=Admin1234!

# Cloud Storage bucket for inventory image uploads (verify exact name in Firebase Console → Storage).
STORAGE_BUCKET=catero-10f39.firebasestorage.app